# Disabled for ITN fork: full Prism multi-platform tag release is not used.
# Windows portable builds: workflow "ITN Windows Portable" + manual GitHub Release assets.
# Re-enable only if you need Linux/macOS/MinGW Prism-style artifacts.
name: Build Application and Make Release

on:
  workflow_dispatch:

permissions: {}

jobs:
  skipped:
    name: Skipped (use ITN Windows Portable)
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "ITN releases are published via ITN Windows Portable + gh release upload."
          echo "This Prism tag-release pipeline is disabled to avoid failed Deployments."
