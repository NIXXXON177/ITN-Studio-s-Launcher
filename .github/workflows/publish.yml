# Disabled for ITN fork: Winget publish needs PrismLauncher.PrismLauncher + WINGET_TOKEN.
name: Publish

on:
  workflow_dispatch:

permissions: {}

jobs:
  skipped:
    name: Skipped (Winget not configured for ITN)
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "Winget publish is disabled on this fork."
